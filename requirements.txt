fastapi==0.115.0
uvicorn[standard]==0.32.0
jinja2==3.1.4
httpx==0.27.2
python-multipart==0.0.12
python-dotenv==1.0.1
sqlalchemy==2.0.35
psycopg[binary]==3.2.10
itsdangerous==2.2.0
xhtml2pdf==0.2.17
# Draws the per-property share cards (app/services/og_image.py). Arrived
# as an xhtml2pdf dependency; pinned here because we now import it
# directly and must not lose it to a change upstream.
pillow==12.3.0
